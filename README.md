# Syntax Highlighting

Renders Markdown-style code in post bodies:

- `` `inline code` `` renders as an inline code chip.
- ` ```lang ` fenced blocks render as syntax-highlighted code blocks (language label optional).

Feature inspiration and code adapted from [Mu Social](https://github.com/eurosky-social/eurosky-social-app) - lots of credit to that team!
